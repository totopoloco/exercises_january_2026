# Disable file system watching (improves debugger performance)
org.gradle.vfs.watch=false

# Disable configuration cache during debugging
org.gradle.configuration-cache=false

# Optional: Reduce daemon idle timeout for debugging
org.gradle.daemon.idletimeout=1800000
